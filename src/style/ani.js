import { keyframes } from 'styled-components'
import * as Ani from 'react-animations';

export const fadeIn = keyframes`${Ani.fadeIn}`;
export const slideInLeft = keyframes`${Ani.slideInLeft}`;
export const slideInRight = keyframes`${Ani.slideInRight}`;
export const slideInUp = keyframes`${Ani.slideInUp}`;
export const slideInDown = keyframes`${Ani.slideInDown}`;